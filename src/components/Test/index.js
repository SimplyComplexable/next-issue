import React from 'react';
import {Container, InnerContainer, OuterContainer} from "./styles";

const Test = ({}) => (
    <Container>
      <OuterContainer>
        <InnerContainer>
          Test
        </InnerContainer>
      </OuterContainer>
    </Container>
)

export default Test;
